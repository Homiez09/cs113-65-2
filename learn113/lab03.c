#include <stdio.h>
#include <stdlib.h>

int main () {
    /* char str[10];
    printf("Enter name: ");
    fgets(str, 10, stdin);

    printf("str = %s\n", str);

    int i = atoi(str);
    double d = atof(str);
    long l = atoi(str);

    printf("%d %lf %ld", i, d, l); */
    
    //-----------------------------------------------------------------------------

    /* int count = 0;

    while (1) {
        count++;
    }  */

    //-----------------------------------------------------------------------------

    /* for (int j = 0; j < 10; j++){

    } */

    int a = 1;
    switch (a){
        case 1:
            printf("a");
            break;
        case 2:
            printf("aa");
            break;
        default:
            printf("-");
            break;
    }
}